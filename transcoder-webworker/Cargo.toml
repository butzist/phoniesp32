[package]
name = "transcoder-webworker"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"

[dependencies]
wasm-bindgen = "=0.2.104"
js-sys = "*"

transcoder = { path = "../transcoder" }
